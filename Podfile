# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FotoLocations' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FotoLocations

  pod 'Firebase'
  pod 'FirebaseDatabase'
  pod 'Kingfisher'
  pod 'FirebaseStorage'
  pod 'FirebaseAuth'
  pod 'SwiftOverlays'
  
  target 'FotoLocationsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FotoLocationsUITests' do
    # Pods for testing
  end

end
